/**
 * Generates a farewell message for a given name
 * @param name - The name to include in the farewell message
 * @returns A farewell message in the format "Goodbye, <name>!"
 */
export function farewell(name: string): string {
  return `Goodbye, ${name}!`
}
